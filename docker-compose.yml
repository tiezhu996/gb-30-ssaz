name: gbadopt

services:
  db:
    image: postgres:16-alpine
    container_name: ${COMPOSE_PROJECT_NAME:-gbadopt}_db
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${DB_NAME:-gbadopt_db}
      POSTGRES_USER: ${DB_USER:-gbadopt_user}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-gbadopt_pwd}
      TZ: Asia/Shanghai
    ports:
      - "${DB_PORT:-5601}:5432"
    volumes:
      - db_data:/var/lib/postgresql/data
      - ./database:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-gbadopt_user} -d ${DB_NAME:-gbadopt_db}"]
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 15s

  redis:
    image: redis:7-alpine
    container_name: ${COMPOSE_PROJECT_NAME:-gbadopt}_redis
    restart: unless-stopped
    ports:
      - "${REDIS_PORT:-6501}:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 20

  minio:
    image: minio/minio:RELEASE.2024-07-04T14-25-45Z
    container_name: ${COMPOSE_PROJECT_NAME:-gbadopt}_minio
    restart: unless-stopped
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER:-minioadmin}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:-minioadmin}
    command: server /data --console-address ":9001"
    ports:
      - "${MINIO_PORT:-9000}:9000"
      - "${MINIO_CONSOLE_PORT:-9001}:9001"
    volumes:
      - minio_data:/data
    healthcheck:
      test: ["CMD-SHELL", "bash -c 'echo > /dev/tcp/127.0.0.1/9000'"]
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 10s

  backend:
    build:
      context: ./backend
    container_name: ${COMPOSE_PROJECT_NAME:-gbadopt}_backend
    restart: unless-stopped
    environment:
      SERVER_PORT: "8080"
      DB_HOST: db
      DB_PORT: "5432"
      DB_USER: ${DB_USER:-gbadopt_user}
      DB_PASSWORD: ${DB_PASSWORD:-gbadopt_pwd}
      DB_NAME: ${DB_NAME:-gbadopt_db}
      JWT_SECRET: ${JWT_SECRET:-change_me_to_a_long_random_string}
      REDIS_ADDR: redis:6379
      MINIO_ENDPOINT: minio:9000
      MINIO_ROOT_USER: ${MINIO_ROOT_USER:-minioadmin}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:-minioadmin}
      MINIO_BUCKET: gbadopt
    ports:
      - "${BACKEND_PORT:-3101}:8080"
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
      minio:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:8080/healthz >/dev/null 2>&1 || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 10s

  frontend:
    build:
      context: ./frontend
    container_name: ${COMPOSE_PROJECT_NAME:-gbadopt}_frontend
    restart: unless-stopped
    ports:
      - "${FRONTEND_PORT:-8101}:80"
    depends_on:
      backend:
        condition: service_healthy

volumes:
  db_data:
  redis_data:
  minio_data:
